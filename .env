# App
PORT=8080

# Database (used to auto-generate DB_URL)
DB_HOST=db
DB_PORT=5432
DB_USER=admin
DB_PASS=admin
DB_NAME=firstdb

# Optional: override the full URL manually
# DB_URL=postgres://admin:admin@db:5432/firstdb?sslmode=disable
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM="noreply@localhost"
